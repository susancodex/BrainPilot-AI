kind = "web"
previewPath = "/"
title = "BrainPilot AI"
version = "1.0.0"
id = "artifacts/brainpilot"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25585

[services.development]
run = "pnpm --filter @workspace/brainpilot run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/brainpilot", "run", "build" ]
publicDir = "artifacts/brainpilot/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25585"
BASE_PATH = "/"
