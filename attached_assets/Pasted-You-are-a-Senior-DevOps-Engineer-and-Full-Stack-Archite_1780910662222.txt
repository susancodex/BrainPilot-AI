You are a Senior DevOps Engineer and Full Stack Architect.

Analyze my entire project and prepare it for production deployment using the following architecture:

Frontend:
- React
- Vite
- TypeScript
- Deploy on Vercel

Backend:
- Django
- Django REST Framework
- Deploy on Render

Database:
- PostgreSQL
- Neon Database

Your task:

1. Analyze the complete project structure.

2. Frontend Setup (Vercel)
   - Verify React + Vite + TypeScript configuration.
   - Fix build issues.
   - Create production environment variable support.
   - Create .env.example.
   - Configure API base URL using environment variables.
   - Ensure npm run build works without errors.
   - Create vercel.json if required.
   - Optimize production build.

3. Backend Setup (Render)
   - Configure Django for production.
   - Install and configure:
     - gunicorn
     - whitenoise
     - dj-database-url
     - psycopg2-binary
   - Create requirements.txt.
   - Create render.yaml.
   - Configure static files.
   - Configure media files.
   - Configure CORS.
   - Configure CSRF trusted origins.
   - Configure ALLOWED_HOSTS using environment variables.
   - Configure DATABASE_URL from environment variables.
   - Create production settings.
   - Ensure migrations run correctly.
   - Ensure collectstatic works correctly.
   - Create startup command for Render:
     gunicorn project_name.wsgi:application

4. Neon PostgreSQL Setup
   - Configure PostgreSQL connection using DATABASE_URL.
   - Add environment variable examples.
   - Ensure local SQLite can still be used for development if needed.
   - Add Neon integration instructions.

5. Environment Files
   Create:
   - frontend/.env.example
   - backend/.env.example

   Include:
   Frontend:
   VITE_API_URL=

   Backend:
   SECRET_KEY=
   DEBUG=False
   DATABASE_URL=
   ALLOWED_HOSTS=
   CORS_ALLOWED_ORIGINS=
   CSRF_TRUSTED_ORIGINS=

6. Security
   - Enable secure headers.
   - Configure HTTPS support.
   - Secure cookies in production.
   - Disable debug mode.
   - Prevent secret exposure.

7. Deployment Documentation
   Create:
   DEPLOYMENT_GUIDE.md

   Include:
   Step 1:
   Create Neon PostgreSQL database.

   Step 2:
   Deploy Django backend to Render.

   Step 3:
   Set Render environment variables.

   Step 4:
   Run migrations.

   Step 5:
   Deploy React frontend to Vercel.

   Step 6:
   Configure frontend environment variables.

   Step 7:
   Verify frontend-backend connection.

8. Validation
   - Fix all deployment issues found.
   - Fix broken imports.
   - Fix dependency issues.
   - Remove unused packages.
   - Ensure production build succeeds.
   - Ensure backend starts successfully.

9. Output
   - Show every file created.
   - Show every file modified.
   - Show final deployment commands.
   - Show all required environment variables.
   - Show Render settings.
   - Show Vercel settings.
   - Show Neon settings.

Do not generate placeholders only.
Actually modify and create all required deployment files in the project.
Make the project production-ready for:
Frontend → Vercel
Backend → Render
Database → Neon PostgreSQL