You are a Senior Software Architect + Principal Backend & Frontend Engineer + DevOps Engineer.
Your task is to fully refactor and productionize my existing full-stack project.
🎯 PRIMARY GOAL
Transform the current codebase into a:
Production-ready
Industry-standard architecture
Fully modular and scalable system
Clean, maintainable codebase (NO vibe coding, NO messy logic)
Fully working CRUD system in all required modules
Ready to deploy on Render (frontend + backend)
🧱 BACKEND REQUIREMENTS (STRICT)
Use clean architecture:
controllers/
services/
routes/
models/
middlewares/
config/
Every module that involves data MUST have full CRUD:
Create
Read (single + list with pagination)
Update
Delete (soft delete preferred)
Must include:
Input validation (schema-based)
Centralized error handling
Authentication (JWT if applicable)
Authorization (role-based if needed)
Environment variables (.env)
Secure headers & best practices
Database:
Proper schema design
Relationships correctly defined
Migrations enabled (if supported)
API standards:
RESTful endpoints
Consistent response format:
{
  "success": true,
  "message": "",
  "data": {}
}
🎨 FRONTEND REQUIREMENTS (STRICT)
Clean folder structure:
components/
pages/
services/api/
hooks/
store/ (if state management used)
API integration must be centralized:
No duplicate fetch calls
Use axios/fetch wrapper
UI must:
Handle loading states
Handle error states
Handle empty states
Authentication flow must persist properly (if present)
Environment variables for API base URL
🧼 CODE QUALITY RULES (VERY IMPORTANT)
NO vibe coding
NO duplicated logic
NO hardcoded values
NO messy or unstructured files
Follow DRY principle
Follow SOLID principles where possible
Write clean, readable, modular code
Proper naming conventions everywhere
🌐 DEPLOYMENT REQUIREMENTS (RENDER)
Prepare full deployment setup for Render:
Backend:
Add render.yaml OR proper manual deployment config
Ensure:
start script (npm start / gunicorn / uvicorn)
build script
correct port binding: process.env.PORT
Production environment variables listed clearly
Frontend:
Build production version
Ensure API URL points to backend Render URL
Configure environment variables properly
📦 DEPENDENCIES CLEANUP
Remove unused packages
Optimize dependencies
Ensure correct versions for production stability
🔐 ENV FILE SETUP
Create .env.example for both frontend and backend:
Backend:
PORT=
DATABASE_URL=
JWT_SECRET=
Frontend:
VITE_API_URL=
🚀 FINAL OUTPUT REQUIRED
At the end, provide:
1. Clean project structure tree
2. All important changed files summary
3. Deployment guide for Render (STEP BY STEP, SIMPLE)
🎯 RENDER DEPLOYMENT GUIDE FORMAT (MUST FOLLOW)
Give steps like:
Backend Deploy
Connect GitHub repo
Select backend service
Add environment variables
Set build command
Set start command
Deploy
Frontend Deploy
Create static site
Add build command
Set publish directory
Add API URL env
Deploy
⚡ FINAL CONDITION
The final system must be:
Fully working
Production ready
Secure
Scalable
Easily deployable on Render in under 5 minutes
If anything in the current project is broken or unstructured, FIX IT FIRST before adding new features.