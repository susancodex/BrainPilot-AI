# Production dependencies — extends base
-r base.txt

# Static files compression + serving (WhiteNoise)
whitenoise>=6.6

# PostgreSQL async driver (faster than psycopg2 on Render)
psycopg[binary]>=3.1

# Groq AI fallback (not in base.txt)
groq>=0.9
